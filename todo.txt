-дополнить Readme.md и добавить счетчик в конец
-удалить todo.txt



---- выло

-контроллер users, который всегда возвращает, что пользователь правильный
-выход из системы
-состояние на хранение залогинен, не залогинен
-сохранение состояния в localStorage

-посты через обзервер, настройка кол-ва постов в одной загрузке (-1 все оставшиеся)

-пользователи на сервере, провека пользователя и guid сессии в памяти
-guid сессии в локальном хранилище, при запуске приложения проверять сессию 
-установка ошибки через класс, а не стиль
-попробовать сделать devexpress стили, как у Димы в приложении (темнозелено-серо-красненькие), 
плоские кнопки и инпуты. сделать отступы.
-ошибка не общая, а для каждого инпута своя с кружком и восклицанием и всплывающей подсказкой 
(красный фон и белый текст)
-окно для ввода поста, растянуть в ширину

декомпозиция:
-структура папок: hooks, pages, router, utils, styles, components (внутри ui и post), api
-страницы в pages, компоненты в components, контролы в components\ui
-обработчики в hooks
-стили в модули

шлифовка:
-крутилка
-убрать лишние заголовки
-бутстраповская стилизация
-синие компоненты (границы, кнопки), цвета взять из синей темы jquery-ui
-скачать авторское решение и сравнить со своим в плане функционала
-почистить консольные логи
-глобальные стили в модули
-notify доделать (сделать функцией, с глобальным элементом в Layout)
-кнопки с иконками
-убрать todo.txt
-дополнить Readme.md и добавить в конец счетчик

проверка:
-свериться с авторским видео